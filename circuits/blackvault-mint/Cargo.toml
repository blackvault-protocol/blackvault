[package]
name = "lockbox_v5_final"
version = "0.1.0"
edition = "2021"

[dependencies]
halo2_proofs = "0.3.1"
ff = "0.13"                 # ← this is the one we needed
blake2b_simd = "1.0"
rand = "0.8"
qrcode = "0.14"
image = "0.25"
serde_json = { version = "1.0", features = ["std"] }
hex = "0.4"